# 2026-09-17

## 架构梳理 + 起步范围讨论（本日未写任何代码）

**工程现状（实测）：** 5 个 pom 已就绪；全工程 0 个 Java 源文件、无 application.yml、无 .git；各模块只有空的 src/main/resources；bank-bootstrap 残留 archetype 生成的空 src/main/webapp/WEB-INF（待清理）。

**确认的架构基线：** 分层单体（非微服务），依赖单向 —— bootstrap → chat → nlu → common，只允许上层依赖下层。父 pom 预留 bank-rag / bank-knowledge / bank-dialog / bank-tool / bank-compliance 五个未来模块，起步阶段不拆。

**请求主链路：** 用户提问 → bank-nlu 七步安检 → 32 类意图 + 槽位抽取 → Redis 会话状态（TTL 1800s，槽位继承 / 知识库锁定）→ bank-chat 意图路由四链路分流（RAG / API / DIALOG / HUMAN）→ RAG 链路 Query 三合一增强 → 向量(Milvus) + BM25(Lucene) 双路召回 → 融合精排 → LLM 生成 → 三级合规校验（事实核查 / 敏感词 / 引用完整性）→ 组装返回。

**起步里程碑：** M0 工程骨架（启动 + 统一响应 + 假回答接口）→ M1 接入与安检（七步流水线 + 四级限流）→ M2 语义（意图 + 槽位 + Redis 会话）→ M3 RAG 闭环（入库 + 双路召回 + 生成 + 合规）。

**阻塞项（等用户回复，答完才能动手）：**
1. 基础设施现状：Redis / MySQL / Milvus 是否就绪，还是需要 Docker 方案
2. LLM 接入方式：DashScope Key / 本地 Ollama / OpenAI 兼容 / 先 Mock
3. 起步范围优先级（用户选）
4. 业务清单来源 —— 简历信息不全处：7 大领域只列 5 个（账户、支付转账、理财投资、贷款、银行卡）；32 类意图清单未知；七步安检只点名 5 步（长度校验、四级限流、Prompt 注入三层检测、PII 分级脱敏、术语归一化），另 2 步未知；PII 四级只说清 CRITICAL（阻断）与 HIGH（脱敏）两级
5. API 链路是连真实银行核心系统，还是全 Mock

**风险：** Spring AI 1.1.8 + Spring AI Alibaba 1.1.2.0 + milvus-sdk-java 2.5.8 组合未实测；本机无 Maven，依赖可解析性待 IDEA reload 验证。

## 用户答复（当日已确认）

1. **基础设施：** 已有 Redis + MySQL；**没有 Milvus** → M3 时再决定（装 Docker Milvus，或先用 Spring AI 内存向量库跑通链路再切）
2. **大模型：** 阿里云百炼 DashScope（与 pom 里的 starter 一致）→ **需要用户提供 API Key**，M0/M1/M2 用不到，M3 才需要；Key 严禁硬编码进仓库
3. **起步范围：** 先打通工程骨架（M0）
4. **业务清单：** 用户手上有现成清单，稍后提供（7 大领域 / 32 类意图 / 七步安检 / PII 分级），不自行编造

**下一步：** 待用户确认 M0 施工清单 + 提供 Redis / MySQL 连接信息后开始写代码。

---

## 业务文档到位 + M0 代码交付（同日稍后）

**用户提供的技术文档：** `D:\xwechat_files\wxid_etyjkgyfjdwy22_7953\msg\file\2026-09\银行文档.md`（保定银行智能客服系统 RAG 检索引擎技术文档，6 章 + 58 道面试题）。**后续所有业务细节以此文档为准，不要自己编。**

**文档补齐了此前全部未知项：**
- **7 大业务领域：** 账户、支付转账、理财投资、贷款、银行卡、网点查询、活动权益
- **32 类意图分布：** 账户 6 / 支付转账 5 / 银行卡 6 / 理财投资 5 / 贷款 5 / 网点服务 3 / 其他 2
- **七步安检（以 2.1.2 编号为准）：** ①基础格式与合法性校验(4001) ②四级并联限流(4002) ③Prompt 注入三层防御(L1 DFA-AC自动机 5000 词 / L2 香农熵 阈值2.5 / L3 DistilBERT 概率>0.7) ④违规敏感内容检测(DFA 2000 词) ⑤PII 四级分级(CRITICAL 阻断 / HIGH 掩码 / MEDIUM 标记截断 / LOW 仅审计) ⑥金融术语归一化(Trie + 正向最大匹配，2000 条) ⑦Query 规范化输出
- **限流阈值：** 用户级 20/分、IP 级 100/分、设备级 50/分（三者 Redis Lua 令牌桶）+ 全局级 5000 QPS（Guava 本地）
- **路由优先级：** API(置信度≥0.95) > RAG(≥0.9 且检索分≥0.7) > DIALOG(0.7~0.9 或缺槽位) > HUMAN(<0.7 或 COMPLAINT/HUMAN_TRANSFER)
- **检索融合：** 0.6×cosine + 0.4×BM25；时间加权 3 月内×1.5 / 6 月内×1.2 / 1 年内×1.0 / 1 年以上×0.8；旧版本×0.5；向量 Top-50 + BM25 Top-20 → Rerank 留 Top-5
- **会话状态：** `session:{sessionId}` Hash，TTL 1800s，Redisson 锁 10s，连续 3 轮未完成自动重置
- **MySQL 表：** conversation_log / intent_stat / complaint
- **错误码：** 4001~4007 对应七步；限流返回 HTTP 429 + retryAfter

**M0 交付方式：** 用户要求"不用帮我写，把代码发出来就行" → **未写入磁盘，仅在对话中输出**（14 个文件：common 6 个、chat 4 个、bootstrap 4 个）。

**发现文档自相矛盾（已告知用户，待其确认）：**
1. 标题与简历写「保定银行」，但正文 1.1 与 Prompt 模板写「邯郸银行」
2. 2.1.1 的执行顺序图漏了第 ④ 步「违规敏感内容检测」（图上只有 8 个节点，未含敏感词检测）

**M0 三个未实测风险：** ①bootstrap pom 里的 spring-ai-starter-model-ollama 在 M0 用不到，若启动报错先注释掉 ②Knife4j 4.5.0 × Spring Boot 3.5 的 springdoc 版本兼容性存疑 ③bank-common 同时引了 lettuce 系与 redisson-starter，实际生效的是 Redisson 连接工厂（lettuce pool 配置无效）

---

## 更正 + 用 Maven 实测复盘（同日 14:00）

**更正：本机是有 Maven 的**（`D:\envs\apache-maven-3.9.14\bin\mvn.cmd`，repo `D:\repository`）。此前记的"本机无 mvn"是错的 —— `mvn -v` 失败只是因为 PATH 里没有，**必须用绝对路径调用 mvn.cmd**。这次已成功实跑。

**实测结果：`mvn -B compile` 全绿** —— 5 个模块 SUCCESS，7.9 秒。之前"bootstrap 0 个 class"是 IDEA 增量编译的中间态，不是真的编不过。教训：**IDEA 的 target 目录状态不能当编译结论用，要跑 Maven 才算**。

**实测确认的问题（用户代码 review）：**
1. **`javax.validation.Valid` 能编过但运行时不生效** —— 根因：`spring-ai-starter-model-ollama` → `spring-ai-client-chat` → 传递带入 `javax.validation:validation-api:1.1.0.Final`。Spring 6 / Hibernate Validator 8 只认 `jakarta.validation.Valid`，所以 `@Valid` 静默失效，`@NotBlank`/`@Size` 不触发。必须改成 jakarta。
2. **ChatController 在 `com.controller`、Knife4jConfig 在 `com.config`**，都在 `com.dz` 之外 → `scanBasePackages="com.dz"` 扫不到 → `/api/ping`、`/api/chat` 会 404。
3. **Knife4jConfig.bankOpenApi() 少了 `@Bean`** → 即使被扫描也不会注册 OpenAPI。
4. **application.yaml 缩进错误**：从 `data:` 起，`data.redis`/`jackson`/`mybatis-plus`/`knife4j`/`springdoc`/`management`/`logging` 全被嵌进 `spring.datasource` 下 → 静默失效（日志 pattern 失效 → 看不到 traceId；logging.level 还残留旧包名 com.bank.ai）。
5. 小问题：TraceIdFilter 第 60 行 `"unknwon"` 拼写；ChatController 混用 `@RequiredArgsConstructor` + `@Resource`；yaml 里 `spring.datasource.createDatabaseIfNotExist` 不是有效属性（URL 参数里已有）。
6. **版本风险实测：** knife4j 4.5.0 传递带入 **springdoc 2.3.0**（面向 Spring Boot 3.2），与 Boot 3.5.15 是否兼容未验证。

**本次交付：** 数据库初始化 SQL（3 张文档点名表 conversation_log / intent_stat / complaint + 2 张我建议补的 intent_config / term_dict），按用户偏好**未写入磁盘，仅在对话中输出**。term_dict 灌了文档里明确给出的 8 条映射；intent_config 只灌文档中"code 与中文名相邻"的 14 条，TRANSFER / CANCEL_TRANSFER 因文档表述含糊留待用户确认。

---

## DashScope key 问题 + M0 全链路实测（同日 14:15）

**用户提问：dashscope API key 在哪里配置。** 未凭记忆回答，全部从本地仓库 jar 反查 + 实跑验证。

**已确认（读 `spring-ai-alibaba-autoconfigure-dashscope-1.1.2.0` / `spring-ai-alibaba-dashscope-1.1.2.0` 源码）：**
- 属性 `spring.ai.dashscope.api-key`；模型级可用 `spring.ai.dashscope.chat.api-key`
- 优先级：模型级 > 公共级 > 环境变量 **`AI_DASHSCOPE_API_KEY`**（有 AI_ 前缀，与官方 SDK 的 DASHSCOPE_API_KEY 不同）
- 默认 base-url `https://dashscope.aliyuncs.com`；默认 chat 模型 `qwen-plus`（`DashScopeApi.DEFAULT_CHAT_MODEL`）
- 自动配置默认全开：`@ConditionalOnDashScopeEnabled` = `@ConditionalOnProperty("spring.ai.dashscope.enabled", matchIfMissing=true)`；chat 那条还有 `matchIfMissing=true` 的模型条件

**实跑三次（`mvn spring-boot:run`，端口 18080）：**
1. 原样启动 → 失败：`dashscopeAgentApi` 的 `IllegalArgumentException: DashScope API key must be set.`
2. 加 `--spring.ai.dashscope.enabled=false` → 失败：`MilvusVectorStoreAutoConfiguration.milvusClient` 连不上 Milvus（`DEADLINE_EXCEEDED`，本机无 Milvus）
3. 再加 `--spring.autoconfigure.exclude=...MilvusVectorStoreAutoConfiguration` → **启动成功，M0 验收通过**

**M0 验收实测结果：**
- `GET /api/ping` → 200，`{"code":0,...,"stage":"M0","traceId":"..."}`，响应头 X-Trace-Id 一致 ✓
- `POST /api/chat` 正常 → 200 统一响应体，answer 占位、intent UNKNOWN、routeChain STUB ✓
- `GET /doc.html` → 200 ✓ —— **knife4j 4.5.0 + springdoc 2.3.0 在 Boot 3.5.15 上能正常打开，此前担心的版本兼容问题没有出现**
- Ollama starter 不阻塞启动 ✓
- 校验确实触发（400），说明用户已把 `javax.validation` 改成 `jakarta` ✓

**用户已自行修复的（我上轮反馈的）：** 包位置（ChatController → com.dz.controller、Knife4jConfig → com.dz.config）、application.yaml 缩进、Knife4jConfig 补 `@Bean`、javax → jakarta。

**仍存在的最后一个问题：`GlobalExceptionHandler` 少了 `@RestControllerAdvice` 注解** → 全局异常处理完全没生效。实测 `question: "a"` 返回的是 Spring 默认错误体 `{"timestamp":...,"status":400,"error":"Bad Request","path":"/api/chat"}`，而不是统一响应体。加上注解即可。
小尾巴：`TraceIdFilter` 第 60 行 `"unknwon"` 拼写仍未改。

**副作用备忘：** 为了让单模块启动能解析到内部依赖，跑过一次 `mvn install -DskipTests`，com.dz:bank-common/nlu/chat/bootstrap 1.0-SNAPSHOT 已装进 `D:\repository`。临时文件 `.build.log` / `.tree.log` / `.run.log` 已清理。

---

## pom 依赖排查（Lombok + Lucene 标红）

**根因（已定位，未改文件，代码输出在对话里）：** 父 pom 自身坐标是 `com.dz:bank`，但 dependencyManagement 里管理的是 `com.bank.ai:bank-*`，而子模块内部依赖引用的是继承来的 `com.dz:bank-*`（不写 version）。groupId 三处对不上 → 三条内部依赖在 dependencyManagement 中匹配不到版本 → Maven ModelValidator 报 `dependencies.dependency.version is missing` → 整个项目依赖树建不起来 → **Lombok 与 lucene-analysis-smartcn 标红是同一个根因的连带症状**，不是它们自己的错。

**佐证：** 本机 `~/.m2/repository` 下连 `spring-boot-starter-parent` 的 pom 都没有，lucene 目录根本不存在 → 项目从未成功 resolve 过；只有 `lombok/1.18.42/lombok-1.18.42.jar` 单独存在。

**坐标 & 版本本身已核实无误：** lucene-analysis-smartcn 10.1.0 中央仓库存在（smartcn 最新 10.4.0，注意 artifact 名 Lucene 9.x 起是 analysis-smartcn，旧名 analyzers-smartcn 在 9+ 不存在）；lombok 1.18.42 jar 已在本地仓库。

**最终处理（用户拍板）：统一成 `com.dz`。** 只改了一处 —— 父 pom dependencyManagement 里三个内部模块的 groupId `com.bank.ai` → `com.dz`（第 125/130/135 行）。父 pom 自身 groupId、四个子模块 `<parent>`、三处内部依赖引用本来就都是 com.dz，无需动。

**已用 Python 脚本静态校验（本机无 mvn，只能这样验）：** 5 个 pom XML 全部 well-formed；三条内部依赖 `com.dz:bank-common/nlu/chat` 现已全部命中父 dependencyManagement；其余不写 version 的依赖均为 spring-boot-starter-parent 及 Spring Cloud / Spring AI / Alibaba BOM 管理的官方 starter，属正常。**待用户在 IDEA 里 Reload All Maven Projects 最终确认。**

**重大更正：Java 包根实际是 `com.dz.*`（不是之前记的 com.bank.ai）**，按层分包：com.dz.api / web / context / exception。现存源码：bank-common 的 R、ErrorCode、BizException、GlobalExceptionHandler、TraceIdFilter、RequestContext；bank-chat 的 ChatRequest。→ **启动类的 `@SpringBootApplication(scanBasePackages = "com.dz")`，后续新建类一律用 com.dz 包根**（MEMORY.md 已同步更正）。

---

## Maven 真机联调（用户已装 Maven 3.9.14，本机可构建）

**环境更正：** Maven 在 `D:\envs\apache-maven-3.9.14`，本地仓库 **`D:\repository`**，JDK `D:\envs\jdk17`，IDEA `D:\JetBrains\IntelliJ IDEA 2026.2.1`。命令行必须写绝对路径且 `-f` 用 Windows 风格路径（Git Bash 里 PATH 是坏的，grep/head/ls 都用不了，只能在命令里跑 python 做过滤）。

**错误 2（真机暴露）：** `spring-ai-alibaba-starter-dashscope` 缺 version。**根因：spring-ai-alibaba-bom 从 1.1.0.0 起到 2.0.0-M1.1，压根不管 dashscope starter**（BOM 只管 graph-core / agent-framework / studio / sandbox / a2a-nacos / config-nacos / graph-observation 七个）。→ 已在父 pom dependencyManagement 新增第 8 节显式锁 `com.alibaba.cloud.ai:spring-ai-alibaba-starter-dashscope = ${spring-ai-alibaba.version}`（1.1.2.0）。该 artifact 本身在中央仓库有 1.1.2.0~2.0.0-M1.1。

**错误 3（真机暴露）：** bank-chat 编译失败 —— `ChatRequest.java` **漏了 import**（@Data / @NotBlank / @Size 三个注解都没引）。已补 `lombok.Data`、`jakarta.validation.constraints.NotBlank`、`jakarta.validation.constraints.Size`。

**最终验证结果：`mvn -B -DskipTests compile` → BUILD SUCCESS**，5 个模块全过，Lombok（1.18.42，靠父 pom 的 annotationProcessorPaths）确认生效；依赖树解析正常：lucene-core/queryparser/analysis-smartcn 全 10.1.0、dashscope 1.1.2.0、milvus store 1.1.8 + sdk 2.5.8、openfeign 4.3.3、mybatis-plus 3.5.15、mysql-connector-j 9.7.0、knife4j 4.5.0、tika 3.3.1。

**遗留观察：** 编译时有 WARNING「选项未被识别: [mapstruct.unmappedTargetPolicy, mapstruct.defaultComponentModel]」。mapstruct-processor 1.6.3 jar 在仓库里是有的，当前工程还没有任何 `@Mapper`，等 M1 写第一个 Mapper 时需确认生成类是否产出；若没生成，就是这个处理器没真正接管。
