# 2026-09-16

## 银行智能客服系统 pom.xml 编写（结合用户简历）

用户简历核心背景（邓钧烨，Java 后端，雄安桃李科技实习）：保定银行智能客服系统 RAG 检索引擎负责人。

**本次完成：**
1. 重写 `bank-chat/pom.xml`（原为 archetype 默认模板）：对话编排层 —— 意图路由四链路分流（RAG/API/DIALOG/HUMAN）、Query 三合一增强、Milvus 向量 + Lucene BM25 双路召回、Tika 知识库入库、OpenFeign API 链路、MapStruct。
2. 重写 `bank-bootstrap/pom.xml`（原为 archetype 默认模板，groupId 还是 com.dz）：启动装配 —— 聚合全模块、MyBatis-Plus + mysql-connector-j、Knife4j、Actuator、Ollama（启用 nlu 的 L3 兜底）、spring-boot-maven-plugin repackage（全工程唯一）。
3. 父 pom 新增 Lucene 10.1.0 版本管理（lucene-core / queryparser / analysis-smartcn）。

**重要修复：** 全部 5 个 pom 的注释分隔线 `<!-- ---------- xxx ---------- -->` 含非法 `--`（XML 规范禁止注释内双连字符，Maven 解析直接 Fatal Error），已批量替换为 `==========`。后续写 pom 注释禁止用双连字符装饰线。

**技术栈版本对（写在父 pom）：** Spring Boot 3.5.15 / Spring Cloud 2025.0.3 / Spring Cloud Alibaba 2025.0.0.0 / Spring AI 1.1.8 / Spring AI Alibaba 1.1.2.0 / Java 17。

**依赖链路：** bootstrap → chat → nlu → common，子模块依赖一律不写 version（BOM / dependencyManagement 统一管理）。

**环境备忘：**
- 本机无 mvn 命令，未做依赖 resolve 验证，需用户在 IDEA 中 reload Maven 确认。
- PowerShell 工具输出捕获异常（stdout 被吞）；Bash 环境缺 dirname/cat/tail 等基础命令。验证脚本写文件 + Read 读取结果的方式可靠。
- Windows 下 Python 读 XML 必须显式 encoding='utf-8'（默认 GBK）。
